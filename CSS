@font-face {
    font-family: 'BancoDoBrasilTitulos';
    src: url('BancoDoBrasilTitulos-Bold.woff') format('woff');
    font-weight: bold;
}

body {
    background-color: #465EFF;
    margin: 0;
    font-family: Arial, sans-serif;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    min-height: 100vh;
    color: #ffffff;
    text-align: center;
}
.main-container {
    display: flex;
    flex-direction: column;
    align-items: flex-start; /* Alinha os elementos à esquerda */
    width: 80%;
    max-width: 1200px;
    margin-left: 50px; /* Adiciona margem à esquerda */
}
.container {
    display: flex;
    align-items: center;
    justify-content: space-between;
    width: 100%;
    margin-bottom: 30px;
}
.texto-alinhado {
    margin-top: 5px;
    font-family: 'BancoDoBrasilTitulos', sans-serif;
    font-size: 2.2rem;
    text-align: left;
    line-height: 1.5;
    max-width: 40%;
    transition: transform 0.3s ease; /* Suaviza a transição do efeito de zoom */
}
.texto-alinhado:hover {
    transform: scale(1.05); /* Aumenta o texto em 5% quando o mouse está sobre ele */
}
.imagem-banco {
    width: 500px; /* Define a largura desejada */
    height: auto; /* Mantém a proporção da imagem */
    border: none; /* Remove o contorno, se houver */
    border-radius: 0; /* Remove o arredondamento das bordas, se houver */
    box-shadow: none; /* Remove o brilho e sombra */
    background-color: transparent; /* Garante que o fundo da imagem é transparente */
    position: fixed; /* Permite posicionar a imagem em relação à janela de visualização */
    top: 0; /* Alinha a imagem ao topo da página */
    right: 0; /* Alinha a imagem ao lado direito da página */
    margin: 250px; /* Adiciona uma margem de 250px do topo e da direita */
    transition: transform 0.3s ease; /* Suaviza a transição do efeito de zoom */
}

.imagem-banco:hover {
    transform: scale(1.10); /* Aumenta a imagem em 10% quando o mouse está sobre ela */
}

.dimac-zone {
    width: 700px; /* Define a largura desejada */
    height: auto; /* Mantém a proporção da imagem */
    border: none; /* Remove o contorno, se houver */
    border-radius: 0; /* Remove o arredondamento das bordas, se houver */
    box-shadow: none; /* Remove o brilho e sombra */
    background-color: transparent; /* Garante que o fundo da imagem é transparente */
    display: block; /* Garante que a imagem é tratada como um bloco */
    margin: 0; /* Remove qualquer margem */
    padding: 0; /* Remove qualquer preenchimento */
    float: left; /* Alinha a imagem à esquerda do contêiner */
    margin-right: 100px; /* Ajusta a margem à direita para separar a imagem do texto */
    margin-top: 0; /* Definir o valor de margin-top, se necessário */
    line-height: 2.0;
    transition: transform 0.3s ease; /* Suaviza a transição do efeito de zoom */
}

.dimac-zone:hover {
    transform: scale(1.1); /* Aumenta a imagem em 10% quando o mouse está sobre ela */
}

.buttons {
    display: flex;
    gap: 100px; /* Ajusta o espaçamento entre os botões */
    justify-content: flex-start; /* Alinha os botões à esquerda */
    width: 65%;
}

.button {
    font-family: 'BancoDoBrasilTitulos', sans-serif; /* Aplica a fonte do Banco do Brasil aos botões */
    font-size: 2rem; /* Corrigido de 2srem para 1rem */
    color: #465EFF;
    background-color: #FCFC30;
    border: none;
    padding: 10px 20px;
    border-radius: 40px;
    cursor: pointer;
    transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease; /* Adiciona transição para o efeito de zoom */
}

.button:hover {
    background-color: #FCFC30;
    box-shadow: 0px 0px 15px rgba(255, 255, 255, 0.5); /* Mantém o brilho apenas nos botões */
    transform: scale(1.3); /* Aumenta o botão em 5% quando o mouse está sobre ele */
}

.button-different {
    font-family: 'BancoDoBrasilTitulos', sans-serif; /* Aplica a fonte do Banco do Brasil ao botão diferente */
    font-size: 2rem; /* Corrigido de 4rem para 2rem */
    color: #465EFF;
    background-color: #FCFC30;
    border: none;
    padding: 10px 20px;
    border-radius: 50px; /* Bordas arredondadas */
    cursor: pointer;
    transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.3s ease; /* Adiciona transição para o efeito de zoom */
    width: 200px; /* Define a largura fixa */
    height: 60px; /* Define a altura fixa */
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
}

.button-different:hover {
    background-color: #FCFC30;
    box-shadow: 0px 0px 15px rgba(255, 255, 255, 0.5); /* Mantém o brilho apenas no botão diferente */
    transform: scale(1.05); /* Aumenta o botão em 5% quando o mouse está sobre ele */
}

.fundo {
    background-image: url('bg.svg');
    background-size: cover;
    background-position: center; /* Centraliza a imagem de fundo */
    background-repeat: no-repeat;
    background-attachment: fixed;
    width: 100%;
    height: 100%;
    position: absolute;
    top: 0;
    left: 0;
    z-index: -1; /* Coloca o fundo atrás de outros elementos */
}

